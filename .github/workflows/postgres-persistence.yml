name: PostgreSQL persistence proof

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  postgres-runtime:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: kurukoo
          POSTGRES_PASSWORD: kurukoo_ci
          POSTGRES_DB: kurukoo_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U kurukoo -d kurukoo_ci"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    env:
      KURUKOO_TEST_POSTGRES_URL: postgres://kurukoo:kurukoo_ci@localhost:5432/kurukoo_ci
      KURUKOO_DOMAIN_POSTGRES_URL: postgres://kurukoo:kurukoo_ci@localhost:5432/kurukoo_domain_ci
      KURUKOO_POSTGRES_SSL: 'false'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci --ignore-scripts --no-audit --no-fund
      - run: npm run lint
      - run: npm run build
      - name: Persistence boundary audit
        run: node scripts/audit-persistence-boundary.mjs
      - name: PostgreSQL shared-writer proof
        run: npx tsx scripts/test-postgres-persistence.ts
      - name: Prepare fresh PostgreSQL domain database
        run: npx tsx scripts/prepare-postgres-domain-db.ts
      - name: Bootstrap canonical PostgreSQL domain schema
        env:
          KURUKOO_TEST_POSTGRES_URL: ${{ env.KURUKOO_DOMAIN_POSTGRES_URL }}
        run: npx tsx scripts/bootstrap-postgres-domain-schema.ts
      - name: PostgreSQL domain runtime proof
        env:
          KURUKOO_TEST_POSTGRES_URL: ${{ env.KURUKOO_DOMAIN_POSTGRES_URL }}
        run: npx tsx scripts/test-postgres-domain-runtime.ts
      - name: PostgreSQL cross-process domain restart proof
        env:
          KURUKOO_TEST_POSTGRES_URL: ${{ env.KURUKOO_DOMAIN_POSTGRES_URL }}
        run: npx tsx scripts/test-postgres-domain-restart.ts
      - name: PostgreSQL migration round-trip audit
        run: npx tsx scripts/test-postgres-migration-roundtrip.ts

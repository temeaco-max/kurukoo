import { getCanonicalStore } from './canonicalStore.js';
import { getCanonicalPersistenceMode } from './canonicalPersistence.js';

async function ensure(sqljsSql:string, postgresSql:string){const s=await getCanonicalStore();await s.run(getCanonicalPersistenceMode()==='postgres'?postgresSql:sqljsSql);return s;}

export async function ensureOrderSchema(){return ensure(`CREATE TABLE IF NOT EXISTS orders(id TEXT PRIMARY KEY,phone TEXT,order_type TEXT,provider_phone TEXT,amount INTEGER,status TEXT,idempotency_key TEXT UNIQUE,created_at TEXT DEFAULT CURRENT_TIMESTAMP,message TEXT,updated_at TEXT DEFAULT CURRENT_TIMESTAMP)`,`CREATE TABLE IF NOT EXISTS orders(id TEXT PRIMARY KEY,phone TEXT,order_type TEXT,provider_phone TEXT,amount INTEGER,status TEXT,idempotency_key TEXT UNIQUE,created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,message TEXT,updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`);}
export async function ensureSavedSchema(){return ensure(`CREATE TABLE IF NOT EXISTS saved_items(id TEXT PRIMARY KEY,phone TEXT NOT NULL,kind TEXT NOT NULL DEFAULT 'context',title TEXT NOT NULL,description TEXT,source_url TEXT,source_id TEXT,metadata TEXT DEFAULT '{}',created_at TEXT DEFAULT CURRENT_TIMESTAMP,updated_at TEXT DEFAULT CURRENT_TIMESTAMP,UNIQUE(phone,kind,source_id))`,`CREATE TABLE IF NOT EXISTS saved_items(id TEXT PRIMARY KEY,phone TEXT NOT NULL,kind TEXT NOT NULL DEFAULT 'context',title TEXT NOT NULL,description TEXT,source_url TEXT,source_id TEXT,metadata TEXT DEFAULT '{}',created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,UNIQUE(phone,kind,source_id))`);}
export async function ensureCartSchema(){const s=await ensure(`CREATE TABLE IF NOT EXISTS cart_items(id TEXT PRIMARY KEY,phone TEXT NOT NULL,offer_id TEXT NOT NULL,title TEXT NOT NULL,seller TEXT,quantity INTEGER NOT NULL DEFAULT 1,price_minor INTEGER,currency TEXT DEFAULT 'NGN',source TEXT,provenance TEXT,external_url TEXT,media_reference TEXT,request_id TEXT,status TEXT DEFAULT 'review',created_at TEXT DEFAULT CURRENT_TIMESTAMP,UNIQUE(phone,offer_id))`,`CREATE TABLE IF NOT EXISTS cart_items(id TEXT PRIMARY KEY,phone TEXT NOT NULL,offer_id TEXT NOT NULL,title TEXT NOT NULL,seller TEXT,quantity INTEGER NOT NULL DEFAULT 1,price_minor INTEGER,currency TEXT DEFAULT 'NGN',source TEXT,provenance TEXT,external_url TEXT,media_reference TEXT,request_id TEXT,status TEXT DEFAULT 'review',created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,UNIQUE(phone,offer_id))`);await s.run(`CREATE TABLE IF NOT EXISTS affiliate_clicks(id INTEGER PRIMARY KEY,product TEXT)`);return s;}
export async function ensureTrustSchema(){const s=await ensure(`CREATE TABLE IF NOT EXISTS trust_score_ledger(id INTEGER PRIMARY KEY AUTOINCREMENT,phone TEXT NOT NULL,score REAL NOT NULL,breakdown_json TEXT NOT NULL,reason TEXT NOT NULL,created_at TEXT DEFAULT CURRENT_TIMESTAMP)`,`CREATE TABLE IF NOT EXISTS trust_score_ledger(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,phone TEXT NOT NULL,score DOUBLE PRECISION NOT NULL,breakdown_json TEXT NOT NULL,reason TEXT NOT NULL,created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`);await s.run('CREATE INDEX IF NOT EXISTS idx_trust_score_ledger_phone ON trust_score_ledger(phone,created_at DESC)');return s;}
export async function ensurePointsSchema(){return ensure(`CREATE TABLE IF NOT EXISTS credit_transactions(id INTEGER PRIMARY KEY AUTOINCREMENT,phone TEXT,amount INTEGER,type TEXT,description TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP)`,`CREATE TABLE IF NOT EXISTS credit_transactions(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,phone TEXT,amount INTEGER,type TEXT,description TEXT,created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`);}
export async function ensureEscrowSchemaCanonical(){return ensure(`CREATE TABLE IF NOT EXISTS escrow(id INTEGER PRIMARY KEY AUTOINCREMENT,order_id TEXT,buyer_phone TEXT,provider_phone TEXT,amount_minor INTEGER,description TEXT,status TEXT DEFAULT 'held',created_at TEXT DEFAULT CURRENT_TIMESTAMP,booking_type TEXT,cooling_off_until TEXT,completed_at TEXT,dispute_reason TEXT,beneficiary_phone TEXT,provider_identity TEXT,delegated_provider INTEGER DEFAULT 0)`,`CREATE TABLE IF NOT EXISTS escrow(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,order_id TEXT,buyer_phone TEXT,provider_phone TEXT,amount_minor BIGINT,description TEXT,status TEXT DEFAULT 'held',created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,booking_type TEXT,cooling_off_until TIMESTAMPTZ,completed_at TIMESTAMPTZ,dispute_reason TEXT,beneficiary_phone TEXT,provider_identity TEXT,delegated_provider BOOLEAN DEFAULT FALSE)`);}
export async function ensureCommercialSchemaCanonical(){const s=await ensure(`CREATE TABLE IF NOT EXISTS commercial_ledger(id INTEGER PRIMARY KEY AUTOINCREMENT,event_type TEXT NOT NULL,direction TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',currency TEXT NOT NULL,gross_minor INTEGER NOT NULL DEFAULT 0,platform_fee_minor INTEGER NOT NULL DEFAULT 0,provider_amount_minor INTEGER NOT NULL DEFAULT 0,processing_fee_minor INTEGER NOT NULL DEFAULT 0,tax_minor INTEGER NOT NULL DEFAULT 0,net_minor INTEGER NOT NULL DEFAULT 0,payer TEXT,payee TEXT,represented_party TEXT,agent_id TEXT,skill TEXT,economic_request_id TEXT,external_reference TEXT,idempotency_key TEXT NOT NULL UNIQUE,metadata TEXT NOT NULL DEFAULT '{}',created_at TEXT DEFAULT CURRENT_TIMESTAMP,settled_at TEXT)`,`CREATE TABLE IF NOT EXISTS commercial_ledger(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,event_type TEXT NOT NULL,direction TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',currency TEXT NOT NULL,gross_minor BIGINT NOT NULL DEFAULT 0,platform_fee_minor BIGINT NOT NULL DEFAULT 0,provider_amount_minor BIGINT NOT NULL DEFAULT 0,processing_fee_minor BIGINT NOT NULL DEFAULT 0,tax_minor BIGINT NOT NULL DEFAULT 0,net_minor BIGINT NOT NULL DEFAULT 0,payer TEXT,payee TEXT,represented_party TEXT,agent_id TEXT,skill TEXT,economic_request_id TEXT,external_reference TEXT,idempotency_key TEXT NOT NULL UNIQUE,metadata TEXT NOT NULL DEFAULT '{}',created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,settled_at TIMESTAMPTZ)`);await s.run('CREATE INDEX IF NOT EXISTS idx_commercial_ledger_request ON commercial_ledger(economic_request_id,created_at DESC)');await s.run('CREATE INDEX IF NOT EXISTS idx_commercial_ledger_status_created ON commercial_ledger(status,created_at DESC)');await s.run(getCanonicalPersistenceMode()==='postgres'?`CREATE TABLE IF NOT EXISTS commercial_fee_rules(code TEXT PRIMARY KEY,mode TEXT NOT NULL,amount_minor BIGINT NOT NULL DEFAULT 0,currency TEXT NOT NULL DEFAULT 'NGN',active BOOLEAN NOT NULL DEFAULT FALSE,description TEXT NOT NULL DEFAULT '',updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`:`CREATE TABLE IF NOT EXISTS commercial_fee_rules(code TEXT PRIMARY KEY,mode TEXT NOT NULL,amount_minor INTEGER NOT NULL DEFAULT 0,currency TEXT NOT NULL DEFAULT 'NGN',active INTEGER NOT NULL DEFAULT 0,description TEXT NOT NULL DEFAULT '',updated_at TEXT DEFAULT CURRENT_TIMESTAMP)`);return s;}
export async function ensurePaymentWebhookSchema(){return ensure(`CREATE TABLE IF NOT EXISTS payment_webhook_events(provider TEXT NOT NULL,event_id TEXT NOT NULL,received_at TEXT DEFAULT CURRENT_TIMESTAMP,PRIMARY KEY(provider,event_id))`,`CREATE TABLE IF NOT EXISTS payment_webhook_events(provider TEXT NOT NULL,event_id TEXT NOT NULL,received_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,PRIMARY KEY(provider,event_id))`);}

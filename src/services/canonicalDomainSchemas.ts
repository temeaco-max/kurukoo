import { getCanonicalStore } from './canonicalStore.js';
import { getCanonicalPersistenceMode } from './canonicalPersistence.js';
const mode=()=>getCanonicalPersistenceMode();
async function schema(sqljsSql:string,postgresSql:string){const s=await getCanonicalStore();await s.run(mode()==='postgres'?postgresSql:sqljsSql);return s;}
export async function ensureOrderSchema(){const s=await schema(`CREATE TABLE IF NOT EXISTS orders(id TEXT PRIMARY KEY,phone TEXT,order_type TEXT,provider_phone TEXT,amount INTEGER,status TEXT,idempotency_key TEXT UNIQUE,created_at TEXT DEFAULT CURRENT_TIMESTAMP,message TEXT,updated_at TEXT DEFAULT CURRENT_TIMESTAMP)`,`CREATE TABLE IF NOT EXISTS orders(id TEXT PRIMARY KEY,phone TEXT,order_type TEXT,provider_phone TEXT,amount INTEGER,status TEXT,idempotency_key TEXT UNIQUE,created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,message TEXT,updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`);try{await s.run('ALTER TABLE orders ADD COLUMN message TEXT')}catch{}try{await s.run(mode()==='postgres'?'ALTER TABLE orders ADD COLUMN updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP':'ALTER TABLE orders ADD COLUMN updated_at TEXT DEFAULT CURRENT_TIMESTAMP')}catch{}return s;}
export async function ensureSavedSchema(){return schema(`CREATE TABLE IF NOT EXISTS saved_items(id TEXT PRIMARY KEY,phone TEXT NOT NULL,kind TEXT NOT NULL DEFAULT 'context',title TEXT NOT NULL,description TEXT,source_url TEXT,source_id TEXT,metadata TEXT DEFAULT '{}',created_at TEXT DEFAULT CURRENT_TIMESTAMP,updated_at TEXT DEFAULT CURRENT_TIMESTAMP,UNIQUE(phone,kind,source_id))`,`CREATE TABLE IF NOT EXISTS saved_items(id TEXT PRIMARY KEY,phone TEXT NOT NULL,kind TEXT NOT NULL DEFAULT 'context',title TEXT NOT NULL,description TEXT,source_url TEXT,source_id TEXT,metadata TEXT DEFAULT '{}',created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,UNIQUE(phone,kind,source_id))`);}
export async function ensureCartSchema(){const s=await schema(`CREATE TABLE IF NOT EXISTS cart_items(id TEXT PRIMARY KEY,phone TEXT NOT NULL,offer_id TEXT NOT NULL,title TEXT NOT NULL,seller TEXT,quantity INTEGER NOT NULL DEFAULT 1,price_minor INTEGER,currency TEXT DEFAULT 'NGN',source TEXT,provenance TEXT,external_url TEXT,media_reference TEXT,request_id TEXT,status TEXT DEFAULT 'review',created_at TEXT DEFAULT CURRENT_TIMESTAMP,UNIQUE(phone,offer_id))`,`CREATE TABLE IF NOT EXISTS cart_items(id TEXT PRIMARY KEY,phone TEXT NOT NULL,offer_id TEXT NOT NULL,title TEXT NOT NULL,seller TEXT,quantity INTEGER NOT NULL DEFAULT 1,price_minor BIGINT,currency TEXT DEFAULT 'NGN',source TEXT,provenance TEXT,external_url TEXT,media_reference TEXT,request_id TEXT,status TEXT DEFAULT 'review',created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,UNIQUE(phone,offer_id))`);await s.run(mode()==='postgres'?`CREATE TABLE IF NOT EXISTS affiliate_clicks(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,product TEXT)`: `CREATE TABLE IF NOT EXISTS affiliate_clicks(id INTEGER PRIMARY KEY AUTOINCREMENT,product TEXT)`);return s;}
export async function ensureTrustSchema(){const s=await schema(`CREATE TABLE IF NOT EXISTS trust_score_ledger(id INTEGER PRIMARY KEY AUTOINCREMENT,phone TEXT NOT NULL,score REAL NOT NULL,breakdown_json TEXT NOT NULL,reason TEXT NOT NULL,created_at TEXT DEFAULT CURRENT_TIMESTAMP)`,`CREATE TABLE IF NOT EXISTS trust_score_ledger(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,phone TEXT NOT NULL,score DOUBLE PRECISION NOT NULL,breakdown_json TEXT NOT NULL,reason TEXT NOT NULL,created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`);await s.run('CREATE INDEX IF NOT EXISTS idx_trust_score_ledger_phone ON trust_score_ledger(phone,created_at DESC)');return s;}
export async function ensurePointsSchema(){return schema(`CREATE TABLE IF NOT EXISTS credit_transactions(id INTEGER PRIMARY KEY AUTOINCREMENT,phone TEXT,amount INTEGER,type TEXT,description TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP)`,`CREATE TABLE IF NOT EXISTS credit_transactions(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,phone TEXT,amount INTEGER,type TEXT,description TEXT,created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`);}
export async function ensureEscrowSchemaCanonical(){return schema(`CREATE TABLE IF NOT EXISTS escrow(id INTEGER PRIMARY KEY AUTOINCREMENT,order_id TEXT,buyer_phone TEXT,provider_phone TEXT,amount_minor INTEGER,description TEXT,status TEXT DEFAULT 'held',created_at TEXT DEFAULT CURRENT_TIMESTAMP,booking_type TEXT,cooling_off_until TEXT,completed_at TEXT,dispute_reason TEXT,beneficiary_phone TEXT,provider_identity TEXT,delegated_provider INTEGER DEFAULT 0)`,`CREATE TABLE IF NOT EXISTS escrow(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,order_id TEXT,buyer_phone TEXT,provider_phone TEXT,amount_minor BIGINT,description TEXT,status TEXT DEFAULT 'held',created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,booking_type TEXT,cooling_off_until TIMESTAMPTZ,completed_at TIMESTAMPTZ,dispute_reason TEXT,beneficiary_phone TEXT,provider_identity TEXT,delegated_provider BOOLEAN DEFAULT FALSE)`);}
export async function ensureCommercialSchemaCanonical(){const s=await schema(`CREATE TABLE IF NOT EXISTS commercial_ledger(id INTEGER PRIMARY KEY AUTOINCREMENT,event_type TEXT NOT NULL,direction TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',currency TEXT NOT NULL,gross_minor INTEGER NOT NULL DEFAULT 0,platform_fee_minor INTEGER NOT NULL DEFAULT 0,provider_amount_minor INTEGER NOT NULL DEFAULT 0,processing_fee_minor INTEGER NOT NULL DEFAULT 0,tax_minor INTEGER NOT NULL DEFAULT 0,net_minor INTEGER NOT NULL DEFAULT 0,payer TEXT,payee TEXT,represented_party TEXT,agent_id TEXT,skill TEXT,economic_request_id TEXT,external_reference TEXT,idempotency_key TEXT NOT NULL UNIQUE,metadata TEXT NOT NULL DEFAULT '{}',created_at TEXT DEFAULT CURRENT_TIMESTAMP,settled_at TEXT)`,`CREATE TABLE IF NOT EXISTS commercial_ledger(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,event_type TEXT NOT NULL,direction TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',currency TEXT NOT NULL,gross_minor BIGINT NOT NULL DEFAULT 0,platform_fee_minor BIGINT NOT NULL DEFAULT 0,provider_amount_minor BIGINT NOT NULL DEFAULT 0,processing_fee_minor BIGINT NOT NULL DEFAULT 0,tax_minor BIGINT NOT NULL DEFAULT 0,net_minor BIGINT NOT NULL DEFAULT 0,payer TEXT,payee TEXT,represented_party TEXT,agent_id TEXT,skill TEXT,economic_request_id TEXT,external_reference TEXT,idempotency_key TEXT NOT NULL UNIQUE,metadata TEXT NOT NULL DEFAULT '{}',created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,settled_at TIMESTAMPTZ)`);await s.run('CREATE INDEX IF NOT EXISTS idx_commercial_ledger_request ON commercial_ledger(economic_request_id,created_at DESC)');await s.run('CREATE INDEX IF NOT EXISTS idx_commercial_ledger_status_created ON commercial_ledger(status,created_at DESC)');await s.run(mode()==='postgres'?`CREATE TABLE IF NOT EXISTS commercial_fee_rules(code TEXT PRIMARY KEY,mode TEXT NOT NULL CHECK(mode IN ('fixed','percent')),amount_minor BIGINT NOT NULL DEFAULT 0,currency TEXT NOT NULL DEFAULT 'NGN',active BOOLEAN NOT NULL DEFAULT FALSE,description TEXT NOT NULL DEFAULT '',updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`: `CREATE TABLE IF NOT EXISTS commercial_fee_rules(code TEXT PRIMARY KEY,mode TEXT NOT NULL CHECK(mode IN ('fixed','percent')),amount_minor INTEGER NOT NULL DEFAULT 0,currency TEXT NOT NULL DEFAULT 'NGN',active INTEGER NOT NULL DEFAULT 0,description TEXT NOT NULL DEFAULT '',updated_at TEXT DEFAULT CURRENT_TIMESTAMP)`);await s.run(`INSERT INTO commercial_fee_rules(code,mode,amount_minor,currency,active,description) VALUES ('platform_transaction','percent',0,'NGN',${mode()==='postgres'?'FALSE':'0'},'Platform transaction fee; set an active rate intentionally before charging.'),('provider_lead','fixed',0,'NGN',${mode()==='postgres'?'FALSE':'0'},'Provider lead fee fallback; skill-specific lead rules may override.'),('agent_subscription','fixed',0,'NGN',${mode()==='postgres'?'FALSE':'0'},'Delegated agent subscription price; explicit product pricing should override.'),('advertising','fixed',0,'NGN',${mode()==='postgres'?'FALSE':'0'},'Advertising spend conversion; campaign budgets must be settled through verified payment.'),('affiliate','percent',0,'NGN',${mode()==='postgres'?'FALSE':'0'},'Affiliate payout rate is partner-specific and must be supplied by verified conversion evidence.') ON CONFLICT(code) DO NOTHING`);await s.run(`CREATE TABLE IF NOT EXISTS commercial_products(code TEXT PRIMARY KEY,name TEXT NOT NULL,product_type TEXT NOT NULL,price_minor ${mode()==='postgres'?'BIGINT':'INTEGER'} NOT NULL DEFAULT 0,currency TEXT NOT NULL DEFAULT 'NGN',billing_period TEXT NOT NULL DEFAULT 'one_time',active ${mode()==='postgres'?'BOOLEAN':'INTEGER'} NOT NULL DEFAULT ${mode()==='postgres'?'TRUE':'1'},metadata TEXT NOT NULL DEFAULT '{}',updated_at ${mode()==='postgres'?'TIMESTAMPTZ':'TEXT'} DEFAULT CURRENT_TIMESTAMP)`);await s.run(`INSERT INTO commercial_products(code,name,product_type,price_minor,currency,billing_period,metadata) VALUES('agent_service','Kurukoo Agent Service','agent_subscription',0,'NGN','monthly','{}') ON CONFLICT(code) DO NOTHING`);await s.run(`CREATE TABLE IF NOT EXISTS affiliate_conversions(id TEXT PRIMARY KEY,partner TEXT NOT NULL,click_id TEXT,external_order_id TEXT,gross_minor ${mode()==='postgres'?'BIGINT':'INTEGER'} DEFAULT 0,commission_minor ${mode()==='postgres'?'BIGINT':'INTEGER'} DEFAULT 0,currency TEXT NOT NULL DEFAULT 'NGN',status TEXT NOT NULL DEFAULT 'pending',created_at ${mode()==='postgres'?'TIMESTAMPTZ':'TEXT'} DEFAULT CURRENT_TIMESTAMP,settled_at ${mode()==='postgres'?'TIMESTAMPTZ':'TEXT'})`);return s;}
export async function ensurePaymentWebhookSchema(){return schema(`CREATE TABLE IF NOT EXISTS payment_webhook_events(provider TEXT NOT NULL,event_id TEXT NOT NULL,received_at TEXT DEFAULT CURRENT_TIMESTAMP,PRIMARY KEY(provider,event_id))`,`CREATE TABLE IF NOT EXISTS payment_webhook_events(provider TEXT NOT NULL,event_id TEXT NOT NULL,received_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,PRIMARY KEY(provider,event_id))`);}

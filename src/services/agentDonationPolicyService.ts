import { getCanonicalStore } from './canonicalStore.js';
import { getCanonicalPersistenceMode } from './canonicalPersistence.js';
export async function ensureAgentDonationPolicySchema():Promise<void>{const db=await getCanonicalStore(),pg=getCanonicalPersistenceMode()==='postgres';await db.run(`CREATE TABLE IF NOT EXISTS agent_donation_policies(agent_id TEXT PRIMARY KEY,enabled ${pg?'BOOLEAN':'INTEGER'} NOT NULL DEFAULT ${pg?'FALSE':'0'},recipient TEXT NOT NULL,recipient_type TEXT NOT NULL DEFAULT 'kurukoo',currency TEXT NOT NULL DEFAULT 'NGN',metadata TEXT NOT NULL DEFAULT '{}',updated_at ${pg?'TIMESTAMPTZ':'TEXT'} DEFAULT CURRENT_TIMESTAMP)`);await db.run(`INSERT INTO agent_donation_policies(agent_id,enabled,recipient,recipient_type,currency,metadata) VALUES('agent_prayer_companion',${pg?'TRUE':'1'},'KURUKOO','kurukoo','NGN','{"purpose":"prayer_companion_support","firstClassProvider":true}') ON CONFLICT(agent_id) DO NOTHING`);}
export async function getAgentDonationPolicy(agentId:string):Promise<any|null>{await ensureAgentDonationPolicySchema();return(await getCanonicalStore()).one<any>('SELECT agent_id,enabled,recipient,recipient_type,currency,metadata FROM agent_donation_policies WHERE agent_id=? LIMIT 1',[agentId]);}

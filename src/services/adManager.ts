import { getCanonicalStore } from './canonicalStore.js';

export interface AdCampaign {
    id: number; title: string; desc: string; imageUrl: string; targetKeyword: string; creditsBudget: number; creditsSpent: number; status: string;
    campaignType?: string; disclosure?: string; advertiserName?: string; firstParty?: number; ctaText?: string; destination?: string; priority?: number;
    targeting?: string; placement?: string; category?: string; country?: string; region?: string; startAt?: string; expiresAt?: string; frequencyCap?: number;
    impressions?: number; clicks?: number; createdAt: string; updatedAt: string; assetStatus?: 'pending'|'approved'|'rejected'; approvedBy?: string;
}
async function ensureAdSchema(){const db=await getCanonicalStore(),pg=db.mode==='postgres';await db.run(`CREATE TABLE IF NOT EXISTS ad_campaigns(id ${pg?'BIGINT GENERATED BY DEFAULT AS IDENTITY':'INTEGER PRIMARY KEY AUTOINCREMENT'},title TEXT NOT NULL,desc TEXT NOT NULL,image_url TEXT NOT NULL,target_keyword TEXT NOT NULL,credits_budget INTEGER NOT NULL,credits_spent INTEGER NOT NULL DEFAULT 0,status TEXT NOT NULL DEFAULT 'active',campaign_type TEXT DEFAULT 'external',disclosure TEXT DEFAULT 'Sponsored',advertiser_name TEXT DEFAULT '',first_party INTEGER DEFAULT 0,cta_text TEXT DEFAULT 'Learn more',destination TEXT DEFAULT '/chat',priority INTEGER DEFAULT 0,targeting TEXT DEFAULT '{}',placement TEXT DEFAULT 'public_discovery',category TEXT DEFAULT 'community',country TEXT DEFAULT 'NG',region TEXT DEFAULT '',start_at ${pg?'TIMESTAMPTZ':'TEXT'},expires_at ${pg?'TIMESTAMPTZ':'TEXT'},frequency_cap INTEGER DEFAULT 3,impressions INTEGER DEFAULT 0,clicks INTEGER DEFAULT 0,created_at ${pg?'TIMESTAMPTZ':'TEXT'} DEFAULT CURRENT_TIMESTAMP,updated_at ${pg?'TIMESTAMPTZ':'TEXT'} DEFAULT CURRENT_TIMESTAMP,asset_status TEXT DEFAULT 'pending',approved_by TEXT DEFAULT '')`);return db;}
export function isApprovedCampaignAsset(imageUrl:unknown,firstParty=0){const image=String(imageUrl||'').trim();if(!image||/unsplash\.com|placehold\.co|placeholder|dummy|data:image/i.test(image))return false;return Number(firstParty)===1?/^\/assets\/[a-z0-9/_-]+\.(?:jpg|jpeg|png|webp|avif|svg)$/i.test(image):/^https?:\/\/[a-z0-9.-]+(?:\/[^\s]*)?$/i.test(image)||/^\/assets\/[a-z0-9/_-]+\.(?:jpg|jpeg|png|webp|avif|svg)$/i.test(image);}
export function isRenderableCampaign(c:Pick<AdCampaign,'imageUrl'|'firstParty'|'assetStatus'>){return c.assetStatus==='approved'&&isApprovedCampaignAsset(c.imageUrl,c.firstParty);}
function mapCampaign(row:Record<string,unknown>):AdCampaign{const m:Record<string,unknown>={};for(const[k,v]of Object.entries(row))m[k.replace(/_([a-z])/g,(_,l:string)=>l.toUpperCase())]=v;return m as unknown as AdCampaign;}
export async function getAdCampaigns():Promise<AdCampaign[]>{const db=await ensureAdSchema();return(await db.all<Record<string,unknown>>('SELECT * FROM ad_campaigns ORDER BY id ASC')).map(mapCampaign);}
export async function getRenderableCampaigns(options:{firstParty?:boolean;placements?:string[];limit?:number;now?:number}={}):Promise<AdCampaign[]>{const now=options.now??Date.now(),placements=options.placements?.length?new Set(options.placements.map(String)):null;const campaigns=(await getAdCampaigns()).filter(c=>c.status==='active'&&(options.firstParty===undefined||Number(c.firstParty)===(options.firstParty?1:0))&&(!placements||placements.has(String(c.placement||'')))&&(!c.startAt||Date.parse(c.startAt)<=now)&&(!c.expiresAt||Date.parse(c.expiresAt)>now)&&isRenderableCampaign(c)).sort((a,b)=>(Number(b.priority)||0)-(Number(a.priority)||0));return options.limit&&options.limit>0?campaigns.slice(0,Math.floor(options.limit)):campaigns;}
export interface UpdateAdCampaignInput{title?:string;desc?:string;imageUrl?:string;targetKeyword?:string;creditsBudget?:number;status?:'active'|'paused'|'completed';campaignType?:string;disclosure?:string;advertiserName?:string;ctaText?:string;destination?:string;placement?:string;category?:string;country?:string;region?:string;startAt?:string|null;expiresAt?:string|null;frequencyCap?:number;priority?:number;targeting?:string;assetStatus?:'pending'|'approved'|'rejected';approvedBy?:string;}
export async function updateAdCampaign(id:number,patch:UpdateAdCampaignInput,actor='admin'):Promise<AdCampaign>{const db=await ensureAdSchema(),current=(await getAdCampaigns()).find(c=>Number(c.id)===Number(id));if(!current)throw new Error('Campaign not found.');const next={...current,...patch}as AdCampaign,title=String(next.title||'').trim().slice(0,160),desc=String(next.desc||'').trim().slice(0,1000),imageUrl=String(next.imageUrl||'').trim().slice(0,500),targetKeyword=String(next.targetKeyword||'').trim().toLowerCase().slice(0,80),disclosure=String(next.disclosure||'').trim().slice(0,120);if(!title||!desc||!targetKeyword||!disclosure)throw new Error('Title, description, target keyword, and disclosure are required.');if(!Number.isFinite(Number(next.creditsBudget))||Number(next.creditsBudget)<0)throw new Error('Credits budget must be a non-negative number.');if(next.assetStatus==='approved'&&!isApprovedCampaignAsset(imageUrl,Number(next.firstParty||0)))throw new Error('Approved status requires a valid approved image asset.');const fields={title,desc,image_url:imageUrl,target_keyword:targetKeyword,credits_budget:Math.floor(Number(next.creditsBudget)),status:next.status||current.status,campaign_type:next.campaignType||current.campaignType||'external',disclosure,advertiser_name:String(next.advertiserName||'').trim().slice(0,160),cta_text:String(next.ctaText||'Learn more').trim().slice(0,80),destination:String(next.destination||'/chat').trim().slice(0,300),placement:String(next.placement||'public_discovery').trim().slice(0,80),category:String(next.category||'community').trim().slice(0,80),country:String(next.country||'NG').trim().slice(0,8),region:String(next.region||'').trim().slice(0,80),start_at:next.startAt||null,expires_at:next.expiresAt||null,frequency_cap:Math.max(1,Math.floor(Number(next.frequencyCap||3))),priority:Math.floor(Number(next.priority||0)),targeting:next.targeting||'{}',asset_status:next.assetStatus||'approved',approved_by:String(next.approvedBy||actor).trim().slice(0,160),updated_at:new Date().toISOString()};const assignments=Object.keys(fields).map(field=>`${field} = ?`).join(', ');await db.run(`UPDATE ad_campaigns SET ${assignments} WHERE id = ?`,[...Object.values(fields),id]);return(await getAdCampaigns()).find(c=>Number(c.id)===Number(id)) as AdCampaign;}
async function getCampaignForTracking(id:number){const campaign=(await getAdCampaigns()).find(c=>Number(c.id)===Number(id));if(!campaign||campaign.status!=='active'||!isRenderableCampaign(campaign))return null;const now=Date.now();if(campaign.startAt&&Date.parse(campaign.startAt)>now)return null;if(campaign.expiresAt&&Date.parse(campaign.expiresAt)<=now)return null;return campaign;}
export async function recordAdImpression(id:number){if(!(await getCampaignForTracking(id)))return false;await(await ensureAdSchema()).run('UPDATE ad_campaigns SET impressions=COALESCE(impressions,0)+1,updated_at=? WHERE id=?',[new Date().toISOString(),id]);return true;}
export async function recordAdClick(id:number){if(!(await getCampaignForTracking(id)))return false;await(await ensureAdSchema()).run('UPDATE ad_campaigns SET clicks=COALESCE(clicks,0)+1,updated_at=? WHERE id=?',[new Date().toISOString(),id]);return true;}
export async function createAdCampaign(campaign:Omit<AdCampaign,'id'|'creditsSpent'|'status'|'createdAt'|'updatedAt'>):Promise<any>{const db=await ensureAdSchema();if(!isApprovedCampaignAsset(campaign.imageUrl,campaign.firstParty))throw new Error('An approved campaign image asset is required.');const firstParty=campaign.firstParty?1:0;if(!String(campaign.disclosure||'').trim())throw new Error('Campaign disclosure is required.');await db.run(`INSERT INTO ad_campaigns(title,desc,image_url,target_keyword,credits_budget,campaign_type,disclosure,advertiser_name,first_party,cta_text,destination,placement,category,country,region,start_at,expires_at,frequency_cap,priority,targeting,asset_status,approved_by) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`,[campaign.title,campaign.desc,campaign.imageUrl,campaign.targetKeyword,campaign.creditsBudget,campaign.campaignType||'external',campaign.disclosure||'Sponsored',campaign.advertiserName||'',firstParty,campaign.ctaText||'Learn more',campaign.destination||'/chat',campaign.placement||'public_discovery',campaign.category||'community',campaign.country||'NG',campaign.region||'',campaign.startAt||null,campaign.expiresAt||null,campaign.frequencyCap||3,campaign.priority||0,campaign.targeting||'{}','approved',firstParty?'Kurukoo':'admin']);return{success:true,message:'Ad campaign created successfully'};}
export async function spendAdCampaign(id:number,cost=2){const db=await ensureAdSchema(),campaign=await db.one<any>('SELECT credits_budget,credits_spent FROM ad_campaigns WHERE id=?',[id]);if(!campaign)return false;const budget=Number(campaign.credits_budget||0),spent=Number(campaign.credits_spent||0),newSpent=spent+cost;if(newSpent>=budget)await db.run(`UPDATE ad_campaigns SET credits_spent=?,status='completed',updated_at=? WHERE id=?`,[budget,new Date().toISOString(),id]);else await db.run('UPDATE ad_campaigns SET credits_spent=?,updated_at=? WHERE id=?',[newSpent,new Date().toISOString(),id]);return true;}
export async function matchAdCampaigns(query:string){const clean=String(query||'').toLowerCase();if(!clean)return[];return(await getRenderableCampaigns()).filter(c=>{const k=String(c.targetKeyword||'').toLowerCase().trim();return Boolean(k)&&clean.includes(k);});}
export async function seedDemoAdCampaigns(){const db=await ensureAdSchema();const campaigns=[['Ask Kurukoo','Tell Kurukoo what you need and keep everything in one conversation.','chat','Open Chat','/chat','public_home','community',100,'/assets/chat/campaign-digital-worker.webp'],['Find useful people and services nearby','Explore eligible providers, agents, events and activity around you.','nearby','Explore Nearby','/discover','public_discovery','community',90,'/assets/chat/campaign-rider-delivery.webp'],['Ask the community','Share or explore useful local questions, reports and experiences.','topics','Explore Topics','/topics','public_content','community',80,'/assets/chat/campaign-food-vendor.webp'],['Know something useful? Contribute it.','Share evidence and context without presenting it as verified fulfilment.','contributor','Become a Contributor','/chat?prompt=I%20want%20to%20contribute%20useful%20local%20information','public_content','community',70,'/assets/chat/campaign-tailor.webp'],['Let Kurukoo keep work moving','Create a bounded agent goal and review its progress, tools and limits.','agent','Explore Agents','/chat?prompt=Show%20me%20my%20agents','public_workspace','agents',60,'/assets/chat/campaign-digital-worker.webp'],['Bring someone to Kurukoo','Invite someone to start a conversation-first Kurukoo relationship.','referral','Invite a Friend','/referral-qr/','public_workspace','community',50,'/assets/chat/campaign-beauty.webp'],['Reach customers through Kurukoo','Explore disclosed, controlled business placements using the same campaign authority.','business','Explore Business','/advertise','public_business','business',40,'/assets/chat/sponsored-home-repair.webp'],['Use Kurukoo where you already are','Review the connected access channels and their current readiness.','channels','Explore Channels','/channels','public_channels','channels',30,'/assets/chat/campaign-food-vendor.webp'],['Support home from wherever you are','Coordinate trusted local help, errands and practical support for family across borders.','diaspora','Ask Kurukoo to coordinate','/chat?prompt=Help%20me%20coordinate%20support%20for%20family%20back%20home','public_diaspora','diaspora',88,'/assets/chat/campaign-diaspora-remittance.webp'],['Keep family care connected','Arrange a thoughtful local check-in or care visit for someone important to you.','care abroad','Arrange a care check-in','/chat?prompt=Help%20me%20arrange%20a%20care%20check-in%20for%20family','public_diaspora','diaspora',84,'/assets/chat/campaign-diaspora-care.webp'],['Trusted eyes on your home','Coordinate verified maintenance and local property support while you are away.','property abroad','Find property support','/chat?prompt=Help%20me%20find%20trusted%20property%20support','public_diaspora','diaspora',80,'/assets/chat/campaign-diaspora-property.webp'],['Plan the moments that matter','Connect celebrations, catering and local coordination with the people you love.','celebration','Plan something together','/chat?prompt=Help%20me%20plan%20a%20family%20celebration','public_diaspora','diaspora',76,'/assets/chat/campaign-diaspora-celebration.webp'],['Back a local business','Help a trusted maker, seller or digital worker serve customers across distance.','support business','Explore local businesses','/chat?prompt=Show%20me%20local%20businesses%20I%20can%20support','public_diaspora','diaspora',72,'/assets/chat/campaign-diaspora-business.webp']];for(const[title,desc,keyword,cta,destination,placement,category,priority,imageUrl]of campaigns){const existing=await db.one<any>('SELECT id FROM ad_campaigns WHERE title=? AND first_party=1 LIMIT 1',[title]);if(existing){await db.run("UPDATE ad_campaigns SET image_url=?,asset_status='approved',approved_by='Kurukoo',updated_at=? WHERE id=?",[imageUrl,new Date().toISOString(),Number(existing.id)]);continue;}await db.run(`INSERT INTO ad_campaigns(title,desc,image_url,target_keyword,credits_budget,status,campaign_type,disclosure,advertiser_name,first_party,cta_text,destination,placement,category,country,region,frequency_cap,priority,targeting,asset_status,approved_by) VALUES(?,?,?,?,0,'active','first_party','Kurukoo promotion','Kurukoo',1,?,?,?,?,?,'',5,?,?,'approved','Kurukoo')`,[title,desc,imageUrl,keyword,cta,destination,placement,category,priority,JSON.stringify({scope:'public',safetyExcluded:true})]);}}

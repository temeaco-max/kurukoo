import crypto from 'node:crypto';import { getCanonicalStore } from './canonicalStore.js';
export type ParticipantKind='human'|'provider'|'contributor'|'agent';export type ContactStatus='active'|'blocked'|'removed';export type CommunicationAction='message'|'call';
export interface PersonProfile{identityId:string;phone:string;displayName:string;avatar:{kind:'placeholder';initials:string;background:string;foreground:string;label:string};participantKind:ParticipantKind;roles:string[];capabilities:string[];presence:'available'|'offline'|'unknown';contributor:boolean;provider:{verified:boolean;type:string;available:boolean}|null;relationship:{status:ContactStatus|'self'|'none';safetyContact:boolean};communication:{message:boolean;call:boolean;callUnavailableReason?:string};}
async function ensureContactSchema(){const db=await getCanonicalStore();const pg=db.mode==='postgres';await db.run(`CREATE TABLE IF NOT EXISTS person_contacts(owner_phone TEXT NOT NULL,person_phone TEXT NOT NULL,label TEXT,status TEXT NOT NULL DEFAULT 'active',created_at ${pg?'TIMESTAMPTZ':'TEXT'} NOT NULL DEFAULT CURRENT_TIMESTAMP,removed_at ${pg?'TIMESTAMPTZ':'TEXT'},PRIMARY KEY(owner_phone,person_phone),CHECK(owner_phone<>person_phone))`);await db.run('CREATE INDEX IF NOT EXISTS idx_person_contacts_owner_status ON person_contacts(owner_phone,status)');await db.run('CREATE INDEX IF NOT EXISTS idx_person_contacts_person ON person_contacts(person_phone,status)');return db;}
function stableColor(identityId:string){return`#${crypto.createHash('sha256').update(identityId).digest('hex').slice(0,6)}`;}export function placeholderAvatar(identityId:string,displayName:string){const initials=displayName.trim().split(/\s+/).filter(Boolean).slice(0,2).map(part=>part[0]!.toUpperCase()).join('')||'?';return{kind:'placeholder' as const,initials,background:stableColor(identityId),foreground:'#FFFFFF',label:`Profile image for ${displayName||'Kurukoo user'}`};}
function participantKind(row:any):ParticipantKind{if(String(row.provider_type||'')==='software_service')return'agent';if(Number(row.is_contributor)===1)return'contributor';if(Number(row.verified_provider)===1||String(row.provider_type||'human')!=='human')return'provider';return'human';}function roles(_row:any,kind:ParticipantKind){const result=['user'];if(kind==='provider')result.push('provider');if(kind==='contributor')result.push('contributor');if(kind==='agent')result.push('agent');return result;}
async function findProfile(phone:string){return(await getCanonicalStore()).one<any>('SELECT phone,name,is_available,is_contributor,verified_provider,provider_type,last_active_at FROM memory_profiles WHERE phone=? LIMIT 1',[phone]);}
export async function getPersonProfile(viewerPhone:string,personPhone:string,action?:CommunicationAction):Promise<PersonProfile|null>{const db=await ensureContactSchema(),person=await findProfile(personPhone);if(!person)return null;const self=viewerPhone===personPhone,contact=self?null:await db.one<any>('SELECT status FROM person_contacts WHERE owner_phone=? AND person_phone=? LIMIT 1',[viewerPhone,personPhone]);if(!self&&contact?.status!=='active')return null;let safety=false;if(!self){try{const row=await db.one<any>(`SELECT id FROM user_safety_contacts WHERE owner_phone=? AND phone=? AND status='active' LIMIT 1`,[viewerPhone,personPhone]);safety=Boolean(row?.id);}catch{}}const kind=participantKind(person),messageAllowed=!self&&contact?.status==='active',callAllowed=messageAllowed&&process.env.KURUKOO_WEBRTC_ENABLED==='true';const profile:PersonProfile={identityId:String(person.phone),phone:String(person.phone),displayName:String(person.name||'Kurukoo user'),avatar:placeholderAvatar(String(person.phone),String(person.name||'Kurukoo user')),participantKind:kind,roles:roles(person,kind),capabilities:[messageAllowed?'message':'',callAllowed?'call':''].filter(Boolean),presence:Number(person.is_available)===1?'available':person.last_active_at?'offline':'unknown',contributor:Number(person.is_contributor)===1,provider:kind==='provider'||kind==='agent'?{verified:Number(person.verified_provider)===1,type:String(person.provider_type||'human'),available:Number(person.is_available)===1}:null,relationship:{status:self?'self':contact?.status||'none',safetyContact:safety},communication:{message:messageAllowed,call:callAllowed,...messageAllowed&&!callAllowed?{callUnavailableReason:'Realtime call transport is unavailable.'}:{}}};if(action==='message'&&!messageAllowed)throw new Error('Message is not authorized for this relationship');if(action==='call'&&!messageAllowed)throw new Error('Call is not authorized for this relationship');return profile;}
export async function addContact(ownerPhone:string,personPhone:string,label?:string){const db=await ensureContactSchema();if(!ownerPhone||!personPhone||ownerPhone===personPhone)throw new Error('A distinct contact identity is required');if(!await findProfile(ownerPhone)||!await findProfile(personPhone))throw new Error('Both identities must exist in Memory Profile');await db.run(`INSERT INTO person_contacts(owner_phone,person_phone,label,status,removed_at) VALUES(?,?,?,'active',NULL) ON CONFLICT(owner_phone,person_phone) DO UPDATE SET label=excluded.label,status='active',removed_at=NULL`,[ownerPhone,personPhone,label?.trim()||null]);return getPersonProfile(ownerPhone,personPhone);}
export async function removeContact(ownerPhone:string,personPhone:string){const result=await(await ensureContactSchema()).run(`UPDATE person_contacts SET status='removed',removed_at=CURRENT_TIMESTAMP WHERE owner_phone=? AND person_phone=? AND status!='removed'`,[ownerPhone,personPhone]);return Number(result.rowCount||0)>0;}
export async function listContacts(ownerPhone:string):Promise<PersonProfile[]>{const rows=await(await ensureContactSchema()).all<any>(`SELECT person_phone FROM person_contacts WHERE owner_phone=? AND status='active' ORDER BY created_at ASC`,[ownerPhone]),profiles:PersonProfile[]=[];for(const row of rows){const profile=await getPersonProfile(ownerPhone,String(row.person_phone));if(profile)profiles.push(profile);}return profiles;}
export async function canCommunicate(ownerPhone:string,personPhone:string,action:CommunicationAction){const profile=await getPersonProfile(ownerPhone,personPhone);if(!profile||!profile.communication[action])return{allowed:false,reason:profile?.communication.callUnavailableReason||`${action} is not authorized for this relationship`};return{allowed:true};}
export async function contactSchemaForTests(_db?:unknown){await ensureContactSchema();}
import { getCanonicalStore } from './canonicalStore.js';
export interface PlatformJourneyEvent{id:string;eventKey:string;contextId?:string;eventType:string;actorPhone?:string;customerPhone?:string;providerPhone?:string;economicRequestId?:string;objectType?:string;objectId?:string;points?:number;metadata?:Record<string,unknown>;occurredAt:string;}
async function ensureSchema(){const db=await getCanonicalStore(),pg=db.mode==='postgres';await db.run(`CREATE TABLE IF NOT EXISTS platform_journey_events(id TEXT PRIMARY KEY,event_key TEXT NOT NULL UNIQUE,context_id TEXT,event_type TEXT NOT NULL,actor_phone TEXT,customer_phone TEXT,provider_phone TEXT,economic_request_id TEXT,object_type TEXT,object_id TEXT,points INTEGER,metadata_json TEXT NOT NULL DEFAULT '{}',occurred_at ${pg?'TIMESTAMPTZ':'TEXT'} NOT NULL DEFAULT CURRENT_TIMESTAMP)`);await db.run('CREATE INDEX IF NOT EXISTS idx_platform_journey_request ON platform_journey_events(economic_request_id,occurred_at DESC)');return db;}
function rowToEvent(row:any):PlatformJourneyEvent{let metadata:Record<string,unknown>={};try{metadata=JSON.parse(String(row.metadata_json||'{}'));}catch{}return{id:String(row.id),eventKey:String(row.event_key),contextId:row.context_id?String(row.context_id):undefined,eventType:String(row.event_type),actorPhone:row.actor_phone?String(row.actor_phone):undefined,customerPhone:row.customer_phone?String(row.customer_phone):undefined,providerPhone:row.provider_phone?String(row.provider_phone):undefined,economicRequestId:row.economic_request_id?String(row.economic_request_id):undefined,objectType:row.object_type?String(row.object_type):undefined,objectId:row.object_id?String(row.object_id):undefined,points:row.points==null?undefined:Number(row.points),metadata,occurredAt:String(row.occurred_at)};}
export async function recordPlatformJourneyEvent(input:Omit<PlatformJourneyEvent,'id'|'occurredAt'>&{occurredAt?:string}):Promise<PlatformJourneyEvent>{const db=await ensureSchema(),eventKey=String(input.eventKey||'').trim();if(!eventKey)throw new Error('Platform journey eventKey is required.');const existing=await db.one<any>('SELECT * FROM platform_journey_events WHERE event_key=? LIMIT 1',[eventKey]);if(existing)return rowToEvent(existing);const id=`pje_${Date.now()}_${Math.random().toString(36).slice(2,9)}`,occurredAt=input.occurredAt||new Date().toISOString();await db.run(`INSERT INTO platform_journey_events(id,event_key,context_id,event_type,actor_phone,customer_phone,provider_phone,economic_request_id,object_type,object_id,points,metadata_json,occurred_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)`,[id,eventKey,input.contextId||null,input.eventType,input.actorPhone||null,input.customerPhone||null,input.providerPhone||null,input.economicRequestId||null,input.objectType||null,input.objectId||null,input.points??null,JSON.stringify(input.metadata||{}),occurredAt]);return{...input,id,eventKey,occurredAt};}
export async function listPlatformJourneyEvents(input:{economicRequestId?:string;phone?:string;limit?:number}={}):Promise<PlatformJourneyEvent[]>{const db=await ensureSchema(),conditions:string[]=[],params:unknown[]=[];if(input.economicRequestId){conditions.push('economic_request_id=?');params.push(input.economicRequestId);}if(input.phone){conditions.push('(actor_phone=? OR customer_phone=? OR provider_phone=?)');params.push(input.phone,input.phone,input.phone);}const where=conditions.length?`WHERE ${conditions.join(' AND ')}`:'',rows=await db.all<any>(`SELECT * FROM platform_journey_events ${where} ORDER BY occurred_at DESC LIMIT ?`,[...params,Math.max(1,Math.min(100,Math.floor(Number(input.limit||50))))]);return rows.map(rowToEvent);}
export async function reconcileEconomicJourney(economicRequestId:string):Promise<number>{const db=await ensureSchema();let created=0;const emit=async(event:Omit<PlatformJourneyEvent,'id'|'occurredAt'>)=>{if(await db.one<any>('SELECT id FROM platform_journey_events WHERE event_key=? LIMIT 1',[event.eventKey]))return;await recordPlatformJourneyEvent(event);created++;};try{const leads=await db.all<any>('SELECT * FROM economic_dispatch_leads WHERE request_id=? ORDER BY created_at ASC',[economicRequestId]);for(const o of leads){const base={economicRequestId,contextId:`economic:${economicRequestId}`,providerPhone:String(o.provider_phone),objectType:'dispatch_lead',objectId:String(o.id),points:Number(o.lead_points||0)||undefined};await emit({...base,eventKey:`dispatch:${o.id}:offered`,eventType:'dispatch_offered'});if(['accepted','arrived','completed'].includes(String(o.status)))await emit({...base,eventKey:`dispatch:${o.id}:accepted`,eventType:'dispatch_accepted'});if(['arrived','completed'].includes(String(o.status)))await emit({...base,eventKey:`dispatch:${o.id}:arrived`,eventType:'dispatch_arrived'});if(String(o.status)==='completed')await emit({...base,eventKey:`dispatch:${o.id}:completed`,eventType:'dispatch_completed'});}}catch{}try{const reviews=await db.all<any>('SELECT * FROM service_reviews WHERE request_id=? ORDER BY created_at ASC',[economicRequestId]);for(const o of reviews)await emit({eventKey:`review:${o.id}`,eventType:'service_reviewed',contextId:`economic:${economicRequestId}`,economicRequestId,customerPhone:String(o.reviewer_phone),providerPhone:String(o.provider_phone),objectType:'service_review',objectId:String(o.id),metadata:{rating:Number(o.rating||0),feedbackPresent:Boolean(o.feedback)}});}catch{}return created;}

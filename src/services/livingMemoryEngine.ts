/** Living Memory Engine — selective retrieval + consolidation lifecycle. */
import { getCanonicalStore } from './canonicalStore.js';
import { getCanonicalPersistenceMode } from './canonicalPersistence.js';
import { persistCoordinatorEvent } from './coordinatorStore.js';
import { getProfile, getMemoryFacts } from './memoryProfile.js';
import { getIntentions } from './deferredRequestService.js';
export type MemoryTier='stable'|'episodic'|'open_intention'|'recent_tail';export type MemoryStatus='active'|'dormant'|'pruned';
export interface MemoryItem{id:string;tier:MemoryTier;text:string;source:string;createdAt:string;relevance:number;mmrScore?:number;accessCount?:number;}
export interface WorkingContextResult{context:string;selected:MemoryItem[];available:MemoryItem[];tokenEstimate:number;intentClass?:string;intentConfidence?:number;}
const MAX_PER_TIER=3,DEFAULT_K=8,MMR_LAMBDA=.7,FASTTEXT_TOKEN_BUDGET=1024,GROQ_TOKEN_BUDGET=2048;
export async function ensureLivingMemorySchema(){const db=await getCanonicalStore(),pg=getCanonicalPersistenceMode()==='postgres';await db.run(`CREATE TABLE IF NOT EXISTS ai_audit_log(id ${pg?'BIGINT GENERATED BY DEFAULT AS IDENTITY':'INTEGER PRIMARY KEY AUTOINCREMENT'},thread_id TEXT,user_phone TEXT NOT NULL,request_text TEXT NOT NULL,intent_class TEXT,intent_confidence REAL,working_context TEXT,available_memory TEXT,selected_memory TEXT,selection_scores TEXT,llm_response TEXT,token_count INTEGER,cost_pence INTEGER DEFAULT 0,created_at ${pg?'TIMESTAMPTZ':'TEXT'} DEFAULT CURRENT_TIMESTAMP)`);await db.run('CREATE INDEX IF NOT EXISTS idx_ai_audit_log_user ON ai_audit_log(user_phone,created_at)');await db.run('CREATE INDEX IF NOT EXISTS idx_ai_audit_log_thread ON ai_audit_log(thread_id,created_at)');for(const[name,type]of Object.entries({open_intentions:"TEXT DEFAULT '[]'",memory_last_consolidated_at:pg?'TIMESTAMPTZ':'TEXT',memory_health_score:'REAL DEFAULT 1.0'})){try{await db.run(`ALTER TABLE memory_profiles ADD COLUMN ${name} ${type}`);}catch{}}for(const[name,type]of Object.entries({memory_tier:"TEXT DEFAULT 'episodic'",access_count:'INTEGER DEFAULT 0',relevance_score:'REAL DEFAULT 0.5',last_accessed_at:pg?'TIMESTAMPTZ':'TEXT',memory_status:"TEXT DEFAULT 'active'",thread_id:'TEXT'})){try{await db.run(`ALTER TABLE messages ADD COLUMN ${name} ${type}`);}catch{}}await db.run('CREATE INDEX IF NOT EXISTS idx_messages_memory_tier ON messages(memory_tier,phone,created_at)');await db.run('CREATE INDEX IF NOT EXISTS idx_messages_memory_status ON messages(memory_status,last_accessed_at)');return db;}
function estimateTokens(text:string){return Math.max(1,Math.ceil(String(text||'').length/4));}
function tokenize(text:string){return new Set(String(text||'').toLowerCase().replace(/[^a-z0-9\s+]/g,' ').split(/\s+/).filter(t=>t.length>2));}
function keywordRelevance(query:string,candidate:string){const q=tokenize(query),c=tokenize(candidate);if(!q.size||!c.size)return .05;let overlap=0;for(const t of q)if(c.has(t))overlap++;const union=new Set([...q,...c]).size,jaccard=overlap/union,density=overlap/Math.max(1,q.size);return Math.min(1,jaccard*.6+density*.4+.05);}
function textSimilarity(a:string,b:string){return keywordRelevance(a,b);}
async function collectStable(phone:string):Promise<MemoryItem[]>{const profile=await getProfile(phone,'living_memory'),facts=await getMemoryFacts(phone,['name','location']),items:MemoryItem[]=[];if(!profile)return items;const nameFact=facts.find(f=>f.field==='name');if(nameFact)items.push({id:`stable:fact:${nameFact.id}`,tier:'stable',text:`User name: ${nameFact.value}`,source:`memory_facts.name:${nameFact.provenance}`,createdAt:nameFact.observedAt,relevance:.3});const locationFact=facts.find(f=>f.field==='location');if(locationFact)items.push({id:`stable:fact:${locationFact.id}`,tier:'stable',text:`Primary location: ${locationFact.value}`,source:`memory_facts.location:${locationFact.provenance}`,createdAt:locationFact.observedAt,relevance:.4});if(profile.inferred_roles)try{const roles=typeof profile.inferred_roles==='string'?JSON.parse(profile.inferred_roles):profile.inferred_roles,list=Array.isArray(roles)?roles:Object.keys(roles||{});if(list.length)items.push({id:'stable:roles',tier:'stable',text:`Inferred roles/skills: ${list.slice(0,8).join(', ')}`,source:'memory_profiles.inferred_roles:inferred',createdAt:profile.updated_at||new Date().toISOString(),relevance:.45});}catch{}if(profile.subscription_tier)items.push({id:'stable:tier',tier:'stable',text:`Subscription: ${profile.subscription_tier}; Points: ${profile.points_balance??profile.wallet_balance_minor??0}`,source:'memory_profiles.subscription:system_state',createdAt:profile.updated_at||new Date().toISOString(),relevance:.25});return items;}
async function collectEpisodic(phone:string,limit=40):Promise<MemoryItem[]>{const db=await ensureLivingMemorySchema(),rows=await db.all<any>(`SELECT id,content,sender,created_at,memory_tier,access_count,relevance_score,memory_status FROM messages WHERE phone=? AND COALESCE(memory_status,'active')='active' AND COALESCE(memory_tier,'episodic') IN ('episodic','stable') ORDER BY id DESC LIMIT ?`,[phone,limit]);return rows.flatMap(r=>{const content=String(r.content||'').trim();return!content||content.length<3?[]:[{id:`msg:${r.id}`,tier:(r.memory_tier==='stable'?'stable':'episodic') as MemoryTier,text:`${r.sender}: ${content.slice(0,400)}`,source:'messages',createdAt:String(r.created_at||''),relevance:Number(r.relevance_score??.5),accessCount:Number(r.access_count??0)}];});}
async function collectOpenIntentions(phone:string):Promise<MemoryItem[]>{return(await getIntentions(phone)).filter((i:any)=>['requested','awaiting_match','partially_matched','open'].includes(String(i.status))).slice(0,5).map((i:any)=>({id:`intention:${i.id}`,tier:'open_intention' as MemoryTier,text:`Open intention: ${i.intent}${i.skill?` (skill: ${i.skill})`:''}${i.location?` @ ${i.location}`:''}. Context: ${String(i.context||'').slice(0,200)}`,source:'open_intentions',createdAt:String(i.created_at||''),relevance:.7}));}
async function collectRecentTail(phone:string,limit=10):Promise<MemoryItem[]>{const db=await ensureLivingMemorySchema(),rows=await db.all<any>('SELECT id,content,sender,created_at FROM messages WHERE phone=? ORDER BY id DESC LIMIT ?',[phone,limit]),items:MemoryItem[]=[];for(const r of rows){const content=String(r.content||'').trim();if(!content)continue;items.push({id:`tail:${r.id}`,tier:'recent_tail',text:`${r.sender}: ${content.slice(0,300)}`,source:'messages.recent',createdAt:String(r.created_at||''),relevance:.6});}return items.reverse();}
export function mmrSelect(query:string,candidates:MemoryItem[],k=DEFAULT_K){if(!candidates.length)return[];const scored=candidates.map(item=>({...item,relevance:Math.max(item.relevance||0,keywordRelevance(query,item.text))}));const remaining=[...scored].sort((a,b)=>b.relevance-a.relevance),selected:MemoryItem[]=[],tierCounts=new Map<string,number>();while(selected.length<k&&remaining.length){for(const item of remaining){const maxSim=selected.reduce((max,s)=>Math.max(max,textSimilarity(item.text,s.text)),0);item.mmrScore=item.relevance-MMR_LAMBDA*maxSim;}remaining.sort((a,b)=>(b.mmrScore??0)-(a.mmrScore??0));const next=remaining.shift()!,count=tierCounts.get(next.tier)||0;if(count>=MAX_PER_TIER)continue;tierCounts.set(next.tier,count+1);selected.push(next);}return selected;}
export function assembleContext(selected:MemoryItem[],maxTokens:number){const parts:string[]=[],seen=new Set<string>();let tokens=0;for(const item of selected){const line=`[${item.tier}] ${item.text}`,dedupeKey=line.trim().replace(/\s+/g,' ').toLowerCase(),t=estimateTokens(line);if(seen.has(dedupeKey)||tokens+t>maxTokens)continue;seen.add(dedupeKey);parts.push(line);tokens+=t;}return{context:parts.join('\n'),tokenEstimate:tokens};}
async function touchSelected(selected:MemoryItem[]){const db=await ensureLivingMemorySchema(),now=new Date().toISOString();for(const item of selected){if(!item.id.startsWith('msg:')&&!item.id.startsWith('tail:'))continue;const msgId=Number(item.id.split(':')[1]);if(!Number.isFinite(msgId))continue;await db.run('UPDATE messages SET access_count=COALESCE(access_count,0)+1,last_accessed_at=?,relevance_score=MIN(1.0,COALESCE(relevance_score,.5)+.05) WHERE id=?',[now,msgId]);}}
export async function buildWorkingContext(phone:string,query:string,options:{intentClass?:string;intentConfidence?:number;route?:'fasttext'|'groq'|'smollm2'|string;threadId?:string}={}):Promise<WorkingContextResult>{await ensureLivingMemorySchema();const[stable,episodic,intentions,recent]=await Promise.all([collectStable(phone),collectEpisodic(phone),collectOpenIntentions(phone),collectRecentTail(phone)]),available=[...stable,...episodic,...intentions,...recent],selected=mmrSelect(query,available,DEFAULT_K),budget=options.route==='fasttext'||options.route==='smollm2'?FASTTEXT_TOKEN_BUDGET:GROQ_TOKEN_BUDGET,{context,tokenEstimate}=assembleContext(selected,budget);await touchSelected(selected);await persistCoordinatorEvent({id:`memory-retrieval:${phone}:${options.threadId||'default'}:${Date.now()}`,type:'memory.context.retrieved',occurredAt:new Date().toISOString(),producer:'livingMemoryEngine',correlationId:`conversation:${options.threadId||'default'}`,ownerPhone:phone.startsWith('anon_')?undefined:phone,payload:{threadId:options.threadId,route:options.route,intentClass:options.intentClass,intentConfidence:options.intentConfidence,selectedCount:selected.length,availableCount:available.length,tokenEstimate,selectedTiers:Array.from(new Set(selected.map(i=>i.tier))),selectedSources:Array.from(new Set(selected.map(i=>i.source)))},sensitivity:phone.startsWith('anon_')?'public':'personal',provenance:{source:'canonical_service',sourceId:options.threadId||phone,evidenceLevel:'persisted_state'},policy:{autonomousAllowed:false,confirmationRequired:'none'},schemaVersion:1});return{context,selected,available,tokenEstimate,intentClass:options.intentClass,intentConfidence:options.intentConfidence};}
export async function logAiAudit(entry:{phone:string;requestText:string;threadId?:string;intentClass?:string;intentConfidence?:number;workingContext:string;available:MemoryItem[];selected:MemoryItem[];llmResponse?:string;tokenCount?:number;costPence?:number}){const db=await ensureLivingMemorySchema();await db.run(`INSERT INTO ai_audit_log(thread_id,user_phone,request_text,intent_class,intent_confidence,working_context,available_memory,selected_memory,selection_scores,llm_response,token_count,cost_pence) VALUES(?,?,?,?,?,?,?,?,?,?,?,?)`,[entry.threadId||null,entry.phone,entry.requestText.slice(0,4000),entry.intentClass||null,entry.intentConfidence??null,entry.workingContext.slice(0,8000),JSON.stringify(entry.available.slice(0,30).map(m=>({id:m.id,tier:m.tier,relevance:m.relevance,text:m.text.slice(0,200)}))),JSON.stringify(entry.selected.map(m=>({id:m.id,tier:m.tier,mmr:m.mmrScore,relevance:m.relevance,text:m.text.slice(0,200)}))),JSON.stringify(entry.selected.map(m=>({id:m.id,relevance:m.relevance,mmr:m.mmrScore}))),entry.llmResponse?entry.llmResponse.slice(0,4000):null,entry.tokenCount??null,entry.costPence??0]);}
export async function withMemoryContext(phone:string|undefined,userPrompt:string,baseSystemPrompt?:string,meta?:{intentClass?:string;intentConfidence?:number;route?:string;threadId?:string}){if(!phone)return{systemPrompt:baseSystemPrompt||''};try{const working=await buildWorkingContext(phone,userPrompt,meta),memoryBlock=working.context?`\n\n--- Living Memory (selective, not full history) ---\n${working.context}\n---`:'';logAiAudit({phone,requestText:userPrompt,threadId:meta?.threadId,intentClass:meta?.intentClass||working.intentClass,intentConfidence:meta?.intentConfidence??working.intentConfidence,workingContext:working.context,available:working.available,selected:working.selected,tokenCount:working.tokenEstimate}).catch(()=>{});return{systemPrompt:`${baseSystemPrompt||'You are Kurukoo, a trusted everyday utility assistant for Nigeria and the diaspora.'}${memoryBlock}`,working};}catch(e){console.error('[LivingMemory] context build failed:',e);return{systemPrompt:baseSystemPrompt||''};}}
export async function runDailyMemoryDecay(){const db=await ensureLivingMemorySchema(),cutoff=new Date(Date.now()-30*24*60*60*1000).toISOString();const result=await db.run(`UPDATE messages SET relevance_score=CASE WHEN COALESCE(relevance_score,.5)*.9<.05 THEN .05 ELSE COALESCE(relevance_score,.5)*.9 END,memory_status=CASE WHEN COALESCE(relevance_score,.5)*.9<.1 THEN 'dormant' ELSE COALESCE(memory_status,'active') END WHERE (last_accessed_at IS NULL OR last_accessed_at<?) AND COALESCE(memory_status,'active')='active' AND COALESCE(memory_tier,'episodic')='episodic'`,[cutoff]);return{updated:Number(result.rowCount||0)};}
export async function runWeeklyMemoryPrune(){const db=await ensureLivingMemorySchema(),cutoff=new Date(Date.now()-90*24*60*60*1000).toISOString(),result=await db.run(`DELETE FROM messages WHERE COALESCE(memory_tier,'episodic')='episodic' AND COALESCE(memory_status,'active')='dormant' AND created_at<?`,[cutoff]);return{deleted:Number(result.rowCount||0)};}
export async function runMemoryCrystallize(){const db=await ensureLivingMemorySchema(),cutoff=new Date(Date.now()-30*24*60*60*1000).toISOString(),rows=await db.all<any>(`SELECT phone,key,COUNT(*) AS observation_count FROM user_behavior_signals WHERE created_at>? AND signal_type IN ('skill_interest','skill_use','accept','complete') GROUP BY phone,key HAVING COUNT(*)>=5`,[cutoff]);let promoted=0;for(const row of rows){const phone=String(row.phone),skill=String(row.key||'').trim().toLowerCase();if(!skill)continue;const existing=await db.one<any>('SELECT id FROM skills WHERE phone=? AND lower(skill)=? LIMIT 1',[phone,skill]);if(existing)await db.run(`UPDATE skills SET source='explicit',confidence=1.0 WHERE phone=? AND lower(skill)=?`,[phone,skill]);else await db.run(`INSERT INTO skills(phone,skill,source,confidence,is_available) VALUES(?,?,?,?,1)`,[phone,skill,'crystallized',.9]);promoted++;}await db.run(`UPDATE memory_profiles SET memory_last_consolidated_at=CURRENT_TIMESTAMP,memory_health_score=COALESCE((SELECT AVG(COALESCE(relevance_score,.5)) FROM messages WHERE messages.phone=memory_profiles.phone AND COALESCE(memory_status,'active')='active'),1.0)`);return{promoted};}
export async function classifyMessageTier(messageId:number,tier:MemoryTier='episodic'){const db=await ensureLivingMemorySchema();await db.run(`UPDATE messages SET memory_tier=?,memory_status='active',relevance_score=COALESCE(relevance_score,.5) WHERE id=?`,[tier,messageId]);}
export async function getWorkingContextInspector(phone:string,requestId?:number){const db=await ensureLivingMemorySchema();if(requestId)return db.one<any>('SELECT * FROM ai_audit_log WHERE id=? AND user_phone=?',[requestId,phone]);return db.all<any>('SELECT * FROM ai_audit_log WHERE user_phone=? ORDER BY id DESC LIMIT 20',[phone]);}

import crypto from 'node:crypto';
import { getCanonicalStore } from './canonicalStore.js';
import { getCanonicalPersistenceMode } from './canonicalPersistence.js';
async function ensureReviewTable(){const s=await getCanonicalStore(),pg=getCanonicalPersistenceMode()==='postgres';await s.run(`CREATE TABLE IF NOT EXISTS unknown_intent_review_queue(id ${pg?'BIGINT GENERATED BY DEFAULT AS IDENTITY':'INTEGER PRIMARY KEY AUTOINCREMENT'},fingerprint TEXT UNIQUE NOT NULL,candidate_text TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',proposed_category TEXT,proposed_skill TEXT,reviewer_id TEXT,reviewer_note TEXT,accepted_training_example TEXT,created_at ${pg?'TIMESTAMPTZ':'TEXT'} DEFAULT CURRENT_TIMESTAMP,reviewed_at ${pg?'TIMESTAMPTZ':'TEXT'})`);return s;}
function normalizeCandidate(text:string){return String(text||'').toLowerCase().replace(/\s+/g,' ').trim().slice(0,1000);}
export async function recordUnknownIntentCandidate(query:string):Promise<void>{const normalized=normalizeCandidate(query);if(!normalized)return;const fingerprint=crypto.createHash('sha256').update(normalized).digest('hex'),s=await ensureReviewTable();await s.run(`INSERT INTO unknown_intent_review_queue(fingerprint,candidate_text) VALUES(?,?) ON CONFLICT(fingerprint) DO UPDATE SET candidate_text=excluded.candidate_text`,[fingerprint,normalized]);}
export async function listUnknownIntentReviewCandidates(status='pending',limit=100):Promise<any[]>{const s=await ensureReviewTable(),safeStatus=String(status||'pending').slice(0,40),safeLimit=Math.max(1,Math.min(250,Math.floor(Number(limit)||100)));return s.all(`SELECT id,fingerprint,candidate_text,status,proposed_category,proposed_skill,reviewer_id,reviewer_note,accepted_training_example,created_at,reviewed_at FROM unknown_intent_review_queue WHERE status=? ORDER BY id ASC LIMIT ?`,[safeStatus,safeLimit]);}
export async function reviewUnknownIntentCandidate(id:number,reviewerId:string,decision:'accepted'|'rejected',proposedCategory?:string,proposedSkill?:string,trainingExample?:string,note?:string):Promise<boolean>{const s=await ensureReviewTable(),status=decision==='accepted'?'accepted':'rejected';return(await s.run(`UPDATE unknown_intent_review_queue SET status=?,proposed_category=?,proposed_skill=?,reviewer_id=?,reviewer_note=?,accepted_training_example=?,reviewed_at=CURRENT_TIMESTAMP WHERE id=?`,[status,proposedCategory||null,proposedSkill||null,reviewerId||null,note||null,decision==='accepted'?trainingExample||null:null,id])).rowCount>0;}

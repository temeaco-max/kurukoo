import { getCanonicalStore } from './canonicalStore.js';
import { processDirectPayment } from './directWallet.js';
import { recordCommercialEvent, ensureCommercialSchema } from './commercialLedger.js';
export async function ensureCommercialBillingSchema():Promise<void>{const s=await getCanonicalStore();await ensureCommercialSchema();await s.run(`CREATE TABLE IF NOT EXISTS subscription_billing_state(owner_phone TEXT PRIMARY KEY,product_code TEXT NOT NULL,tier TEXT NOT NULL,price_minor INTEGER NOT NULL,currency TEXT NOT NULL,next_billing_date TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'active',failure_count INTEGER NOT NULL DEFAULT 0,last_payment_reference TEXT,updated_at TEXT DEFAULT CURRENT_TIMESTAMP)`);await s.run('CREATE INDEX IF NOT EXISTS idx_subscription_billing_due ON subscription_billing_state(status,next_billing_date)');}
export async function upsertSubscriptionBilling(input:{ownerPhone:string;productCode:string;tier:string;priceMinor:number;currency:string;nextBillingDate:Date}):Promise<void>{await ensureCommercialBillingSchema();const s=await getCanonicalStore(),nextBillingDate=input.nextBillingDate.toISOString();await s.run(`INSERT INTO subscription_billing_state(owner_phone,product_code,tier,price_minor,currency,next_billing_date,status,failure_count,updated_at) VALUES(?,?,?,?,?,?,'active',0,CURRENT_TIMESTAMP) ON CONFLICT(owner_phone) DO UPDATE SET product_code=excluded.product_code,tier=excluded.tier,price_minor=excluded.price_minor,currency=excluded.currency,next_billing_date=excluded.next_billing_date,status='active',failure_count=0,updated_at=CURRENT_TIMESTAMP`,[input.ownerPhone,input.productCode,input.tier,input.priceMinor,input.currency,nextBillingDate]);}
export async function runRecurringSubscriptionBillingPass():Promise<{attempted:number;renewed:number;failed:number}>{await ensureCommercialBillingSchema();const s=await getCanonicalStore(),rows=await s.all<any>(`SELECT owner_phone,product_code,tier,price_minor,currency,next_billing_date,failure_count FROM subscription_billing_state WHERE status='active' AND next_billing_date<=? ORDER BY next_billing_date ASC LIMIT 100`,[new Date().toISOString()]);let attempted=0,renewed=0,failed=0;for(const row of rows){attempted++;const owner=String(row.owner_phone),amount=Number(row.price_minor||0),currency=String(row.currency||'NGN').toUpperCase();if(amount<=0){renewed++;continue;}const paid=await processDirectPayment(owner,'SYSTEM',amount/100);if(paid){const next=new Date();next.setMonth(next.getMonth()+1);await s.run(`UPDATE subscription_billing_state SET next_billing_date=?,status='active',failure_count=0,updated_at=CURRENT_TIMESTAMP WHERE owner_phone=?`,[next.toISOString(),owner]);await recordCommercialEvent({eventType:'subscription_charge',direction:'inbound',status:'settled',currency,grossMinor:amount,platformFeeMinor:amount,payer:owner,payee:'KURUKOO',representedParty:owner,externalReference:`renewal:${owner}:${next.toISOString()}`,idempotencyKey:`subscription:renewal:${owner}:${row.product_code}:${String(row.next_billing_date)}`,metadata:{tier:row.tier,productCode:row.product_code,billingPeriod:'monthly'}});renewed++;}else{const failures=Number(row.failure_count||0)+1,status=failures>=3?'past_due':'active';await s.run(`UPDATE subscription_billing_state SET status=?,failure_count=?,updated_at=CURRENT_TIMESTAMP WHERE owner_phone=?`,[status,failures,owner]);failed++;}}return{attempted,renewed,failed};}

import { Router } from 'express';
import fs from 'fs/promises';
import path from 'path';
import crypto from 'crypto';
import { authenticateUser, optionalAuthenticateUser, type AuthRequest } from '../middleware/auth.js';
import { deleteChatMessage, listChatConversations, listChatMessages, clearChatConversation, ensureConversation, appendChatMessage } from '../services/chatConversationService.js';
import { migrateGuestSessionToAccount } from '../services/guestSessionMigration.js';
import { applyQrReferralAttribution } from '../services/qrContextService.js';
import { processCanonicalChatTurn } from '../services/canonicalChatTurnService.js';
import { getAuthState, setAuthState } from '../services/conversationalAuthService.js';
import economicRequestRouter from './economicRequestRouter.js';
import { listUniversalCapabilities } from '../services/universalCapabilityProtocol.js';
import { executeCanonicalCapabilityProposal } from '../services/canonicalCapabilityExecutor.js';
import { createChatAttachment, getChatAttachment, deleteChatAttachment, resolveChatAttachmentPath } from '../services/chatAttachmentService.js';

const router=Router();
router.post('/auth/start',optionalAuthenticateUser,async(req:AuthRequest,res)=>{const phone=userPhone(req,res);if(!phone)return res.status(401).json({error:'Guest session is unavailable'});if(!phone.startsWith('anon_'))return res.json({success:true,authenticated:true});const current=await getAuthState(phone);if(current.state==='none')await setAuthState(phone,'awaiting_name',{});res.json({success:true,state:current.state==='none'?'awaiting_name':current.state});});
router.all('/auth/*',(_req,res)=>res.status(404).json({error:'Not found'}));
function getGuestPhone(req:any,res:any){const cookie=String(req.headers.cookie||'');const pair=cookie.split(';').map(v=>v.trim()).find(v=>v.startsWith('kurukoo_guest_id='));if(pair)return decodeURIComponent(pair.slice(17));const id=`anon_${crypto.randomUUID()}`,secure=process.env.NODE_ENV==='production'?'; Secure':'';res.setHeader('Set-Cookie',`kurukoo_guest_id=${id}; Path=/; HttpOnly; SameSite=Lax; Max-Age=604800${secure}`);return id;}
function userPhone(req:AuthRequest,res?:any):string|null{if(req.user?.phone)return String(req.user.phone);if(res)return getGuestPhone(req,res);return null;}
router.use('/economic-requests',authenticateUser,economicRequestRouter);
router.post('/action',authenticateUser,async(req:AuthRequest,res)=>{const phone=userPhone(req);const body=req.body?.proposal&&typeof req.body.proposal==='object'?{...req.body.proposal,...req.body}:req.body||{};if(!phone||phone.startsWith('anon_'))return res.status(401).json({error:'Authenticated owner is required'});if(typeof body.capability!=='string'||typeof body.action!=='string')return res.status(400).json({error:'capability and action are required'});try{const result=await executeCanonicalCapabilityProposal({phone,capability:body.capability.slice(0,120),action:body.action.slice(0,120),contextId:typeof body.contextId==='string'?body.contextId.slice(0,180):undefined,canonicalObjectId:typeof body.canonicalObjectId==='string'?body.canonicalObjectId.slice(0,180):undefined,arguments:body.arguments&&typeof body.arguments==='object'?body.arguments:undefined,confidence:body.confidence,reason:typeof body.reason==='string'?body.reason.slice(0,500):undefined,confirmationRequired:Boolean(body.confirmationRequired),confirmationGranted:Boolean(body.confirmationGranted||body.confirmed),idempotencyKey:typeof body.idempotencyKey==='string'?body.idempotencyKey.slice(0,180):undefined,conversationId:typeof body.conversationId==='string'?body.conversationId.slice(0,180):undefined,channel:typeof body.channel==='string'?body.channel.slice(0,30):undefined});const statusCode=result.status==='unauthorized'?403:result.status==='invalid'?400:result.status==='stale_context'?409:200;res.status(statusCode).json({success:!['invalid','unauthorized','stale_context','failed','blocked'].includes(result.status),protocol:'kurukoo-universal-capability-v1',result});}catch(error){console.error('[Chat] canonical capability action failed:',error);res.status(500).json({error:'Unable to execute the canonical capability action'});}});
router.get('/capabilities',async(req:AuthRequest,res)=>{try{const requestedMode=typeof req.query.mode==='string'?req.query.mode:undefined,catalog=await listUniversalCapabilities(),capabilities=requestedMode?catalog.filter(item=>item.mode===requestedMode):catalog;res.json({success:true,protocol:'kurukoo-universal-capability-v1',canonicalOwners:['canonicalChatTurnService','canonical domain services','agentRuntime'],capabilities});}catch(error){console.error('[Chat] capability catalog failed:',error);res.status(500).json({error:'Unable to load the canonical capability catalog'});}});
function sse(res:any,payload:any){res.write(`data: ${JSON.stringify(payload)}\n\n`);}function chunkText(text:string){const chunks:string[]=[];const source=String(text||'');for(let i=0;i<source.length;i+=24)chunks.push(source.slice(i,i+24));return chunks;}
router.post('/stream',optionalAuthenticateUser,async(req:AuthRequest,res)=>{const phone=userPhone(req,res),message=typeof req.body?.message==='string'?req.body.message.trim():'',channel=typeof req.body?.channel==='string'?req.body.channel.slice(0,30):'web',conversationId=typeof req.body?.conversationId==='string'?req.body.conversationId:undefined,attachment=req.body?.attachment,contextAction=req.body?.contextAction&&typeof req.body.contextAction==='object'?{type:typeof req.body.contextAction.type==='string'?req.body.contextAction.type.slice(0,80):'',entityId:typeof req.body.contextAction.entityId==='string'?req.body.contextAction.entityId.slice(0,180):undefined,contextId:typeof req.body.contextAction.contextId==='string'?req.body.contextAction.contextId.slice(0,180):undefined,conversationId:typeof req.body.contextAction.conversationId==='string'?req.body.contextAction.conversationId.slice(0,180):undefined,canonicalAction:typeof req.body.contextAction.canonicalAction==='string'?req.body.contextAction.canonicalAction.slice(0,120):undefined,objectType:typeof req.body.contextAction.objectType==='string'?req.body.contextAction.objectType.slice(0,80):undefined,objectId:typeof req.body.contextAction.objectId==='string'?req.body.contextAction.objectId.slice(0,180):undefined}:undefined;if(!phone||!message)return res.status(400).json({error:'Message is required'});res.setHeader('Content-Type','text/event-stream; charset=utf-8');res.setHeader('Cache-Control','no-cache, no-transform');res.setHeader('Connection','keep-alive');res.setHeader('X-Accel-Buffering','no');let fullReply='',cardData:any=null,activeConversation=conversationId;try{const turn=await processCanonicalChatTurn({phone,message,channel,conversationId,attachment,contextAction});activeConversation=turn.conversationId;fullReply=turn.reply;cardData=turn.cardData;if(turn.authSuccess){const secure=process.env.NODE_ENV==='production'?'; Secure':'';res.setHeader('Set-Cookie',['kurukoo_auth='+encodeURIComponent(turn.authSuccess.token)+'; Path=/; HttpOnly; SameSite=Lax; Max-Age=604800'+secure,'kurukoo_guest_id=; Path=/; HttpOnly; SameSite=Lax; Max-Age=0'+secure]);await migrateGuestSessionToAccount(phone,turn.authSuccess.phone);await applyQrReferralAttribution(phone,turn.authSuccess.phone).catch(()=>undefined);}res.flushHeaders?.();sse(res,{type:'status',status:'processing',label:'Kurukoo is checking your request…',grounded:true});sse(res,{type:'conversation',conversationId:activeConversation,messageId:turn.userMessageId});sse(res,{type:'status',status:'typing',label:'Kurukoo is typing…'});if(turn.progressStage&&turn.progressStage!=='complete')sse(res,{type:'progress',stage:turn.progressStage,label:turn.progressStage==='understanding'?'Understanding your request…':turn.progressStage==='checking'?'Checking the available Kurukoo state…':turn.progressStage==='coordinating'?'Preparing the next supported step…':'Preparing your request…',grounded:true});if(turn.authSuccess)sse(res,{type:'auth_success',phone:turn.authSuccess.phone});if(turn.agentGoal)sse(res,{type:'agent_goal',goal:turn.agentGoal});if(turn.capabilityResult)sse(res,{type:'capability_result',result:turn.capabilityResult});for(const chunk of chunkText(fullReply))sse(res,{type:'text',content:chunk});sse(res,{type:'status',status:'complete'});sse(res,{type:'done',fullReply:fullReply.trim(),cardData,conversationId:activeConversation,diagnostics:{classificationSource:turn.classificationSource,intentConfidence:turn.intentConfidence,modelProvider:turn.modelProvider,model:turn.model,latencyMs:turn.latencyMs,extractionSource:turn.extractionSource,extractedEntities:turn.extractedEntities,canonicalAction:turn.canonicalAction,progressStage:turn.progressStage,contextDecision:turn.contextDecision?{selectedContext:turn.contextDecision.selectedContext,selectedContextId:turn.contextDecision.selectedContextId,relation:turn.contextDecision.relation,confidence:turn.contextDecision.confidence,ambiguous:turn.contextDecision.ambiguous,preserveContextIds:turn.contextDecision.preserveContextIds}:undefined,capabilityResult:turn.capabilityResult,finalState:cardData?.status||cardData?.state||cardData?.stage}});sse(res,'[DONE]');res.end();}catch(error){console.error('[Chat] unified stream failed:',error);if(!res.writableEnded){sse(res,{type:'status',status:'error'});sse(res,{type:'error',error:'Unable to complete your request right now.'});sse(res,'[DONE]');res.end();}}});
router.get('/history',optionalAuthenticateUser,async(req:AuthRequest,res)=>{const phone=userPhone(req,res);if(!phone)return res.status(401).json({error:'Phone is required'});const conversationId=typeof req.query.conversationId==='string'?req.query.conversationId:undefined,beforeId=typeof req.query.beforeId==='string'?Number(req.query.beforeId):undefined,limit=typeof req.query.limit==='string'?Number(req.query.limit):50;try{const[conversations,messages]=await Promise.all([listChatConversations(phone,50),listChatMessages(phone,{conversationId,beforeId,limit})]);res.json({success:true,conversations,messages,nextBeforeId:messages.length?messages[0].id:null});}catch(error){console.error('[Chat] history failed:',error);res.status(500).json({error:'Unable to load conversation history'});}});
router.post('/conversation',optionalAuthenticateUser,async(req:AuthRequest,res)=>{const phone=userPhone(req,res);if(!phone)return res.status(401).json({error:'Phone is required'});const channel=typeof req.body?.channel==='string'?req.body.channel.slice(0,30):'web',title=typeof req.body?.title==='string'?req.body.title.slice(0,120):undefined;try{const id=await ensureConversation(phone,undefined,channel,title,true);res.json({success:true,conversationId:id});}catch(error){console.error('[Chat] conversation create failed:',error);res.status(500).json({error:'Unable to create conversation'});}});
router.delete('/message/:id',async(req:AuthRequest,res)=>{const phone=userPhone(req),id=Number(req.params.id);if(!phone||!Number.isInteger(id)||id<=0)return res.status(400).json({error:'Invalid message ID'});try{const deleted=await deleteChatMessage(phone,id);if(!deleted)return res.status(404).json({error:'Message not found'});res.json({success:true});}catch(error){res.status(500).json({error:'Unable to delete message'});}});
router.delete('/conversation/:id',async(req:AuthRequest,res)=>{const phone=userPhone(req),conversationId=String(req.params.id||'');if(!phone||!conversationId)return res.status(400).json({error:'Invalid conversation ID'});try{res.json({success:true,deleted:await clearChatConversation(phone,conversationId)});}catch(error){res.status(500).json({error:'Unable to delete conversation'});}});
router.post('/attachments',async(req:AuthRequest,res)=>{const phone=userPhone(req);if(!phone)return res.status(401).json({error:'Authenticated phone is required'});const{name,type,data}=req.body||{};if(typeof name!=='string'||typeof type!=='string'||typeof data!=='string')return res.status(400).json({error:'name, type and data are required'});const raw=data.replace(/^data:[^;]+;base64,/,'');if(!/^[A-Za-z0-9+/]*={0,2}$/.test(raw)||raw.length%4===1)return res.status(400).json({error:'Attachment data is not valid base64'});try{const attachment=await createChatAttachment({phone,name,type,data:Buffer.from(raw,'base64')});res.status(201).json({success:true,attachment:{...attachment,url:`/api/chat/attachments/${attachment.id}`,expiresInDays:30}});}catch(error){const message=error instanceof Error?error.message:'Unable to store attachment';res.status(/unsupported/.test(message)?415:/exceeds/.test(message)?413:400).json({error:message});}});
router.get('/attachments/:id',authenticateUser,async(req:AuthRequest,res)=>{const phone=userPhone(req),id=String(req.params.id||'');if(!phone||!/^[0-9a-f-]{36}$/i.test(id))return res.status(404).end();const row=await getChatAttachment(phone,id);if(!row)return res.status(404).end();const resolved=resolveChatAttachmentPath(String(row.stored_path));if(!resolved)return res.status(404).end();res.type(String(row.mime_type));res.setHeader('Content-Disposition',`inline; filename="${String(row.original_name).replace(/["\r\n]/g,'')}"`);res.setHeader('Content-Length',String(row.size_bytes));res.sendFile(resolved);});
router.delete('/attachments/:id',authenticateUser,async(req:AuthRequest,res)=>{const phone=userPhone(req),id=String(req.params.id||'');if(!phone||!/^[0-9a-f-]{36}$/i.test(id))return res.status(404).end();const deleted=await deleteChatAttachment(phone,id);if(!deleted)return res.status(404).end();res.json({success:true});});
export default router;

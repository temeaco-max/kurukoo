# Prayer Companion Status

The Prayer Companion is implemented as a first-class agent plus canonical capability. External voice providers remain configuration-dependent; missing keys fail closed. Text generation remains available through the existing Kurukoo AI boundary.